class EventModel {
  int id;
  String name, description, address, startTime, endTime;
  DateTime date;
  EventModel({
    required this.id,
    required this.name,
    required this.description,
    required this.address,
    required this.date,
    required this.startTime,
    required this.endTime,
  });
}
